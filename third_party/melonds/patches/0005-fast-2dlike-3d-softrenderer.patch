diff --git a/src/GPU3D.cpp b/src/GPU3D.cpp
index 4a1426aa..0ab0bf09 100644
--- a/src/GPU3D.cpp
+++ b/src/GPU3D.cpp
@@ -2501,7 +2501,12 @@ void GPU3D::VBlank() noexcept
                     && memcmp(RenderToonTable, ToonTable, 32*2) == 0;
             }
 
-            RenderDispCnt = DispCnt;
+            {
+                u32 disp = DispCnt;
+                if (Fast2DApprox())
+                    disp &= ~((1u<<4) | (1u<<5) | (1u<<7)); // no AA, edge marking, fog
+                RenderDispCnt = disp;
+            }
             RenderAlphaRef = AlphaRef;
 
             memcpy(RenderEdgeTable, EdgeTable, 8*2);
@@ -2995,6 +3000,11 @@ void GPU3D::Write32(u32 addr, u32 val) noexcept
     Log(LogLevel::Debug, "unknown GPU3D write32 %08X %08X\n", addr, val);
 }
 
+bool GPU3D::Fast2DApprox() const noexcept
+{
+    return NDS.Speedhacks.Fast3D && NDS.Speedhacks.SpeedFirst;
+}
+
 void GPU3D::Blit(const GPU& gpu) noexcept
 {
     if (CurrentRenderer)
diff --git a/src/GPU3D.h b/src/GPU3D.h
index d10df55f..93f66daf 100644
--- a/src/GPU3D.h
+++ b/src/GPU3D.h
@@ -121,6 +121,9 @@ public:
     [[nodiscard]] const Renderer3D& GetCurrentRenderer() const noexcept { return *CurrentRenderer; }
     void SetCurrentRenderer(std::unique_ptr<Renderer3D>&& renderer) noexcept;
 
+    /// When true, software renderer skips fog/edge/AA and simplifies translucent blending (speed over accuracy).
+    [[nodiscard]] bool Fast2DApprox() const noexcept;
+
     u8 Read8(u32 addr) noexcept;
     u16 Read16(u32 addr) noexcept;
     u32 Read32(u32 addr) noexcept;
diff --git a/src/GPU3D_Soft.cpp b/src/GPU3D_Soft.cpp
index a9d0bd64..dcb34e83 100644
--- a/src/GPU3D_Soft.cpp
+++ b/src/GPU3D_Soft.cpp
@@ -936,6 +936,7 @@ void SoftRenderer::RenderShadowMaskScanline(const GPU3D& gpu3d, RendererPolygon*
 void SoftRenderer::RenderPolygonScanline(const GPU& gpu, RendererPolygon* rp, s32 y)
 {
     Polygon* polygon = rp->PolyData;
+    const bool fast2d = gpu.GPU3D.Fast2DApprox();
 
     u32 polyattr = (polygon->Attr & 0x3F008000);
     if (!polygon->FacingView) polyattr |= (1<<4);
@@ -1193,8 +1194,7 @@ void SoftRenderer::RenderPolygonScanline(const GPU& gpu, RendererPolygon* rp, s3
             if (!(polygon->Attr & (1<<11))) z = -1;
             PlotTranslucentPixel(gpu.GPU3D, pixeladdr, color, z, polyattr, polygon->IsShadow);
 
-            // blend with bottom pixel too, if needed
-            if ((dstattr & 0xF) && (pixeladdr < BufferSize))
+            if (!fast2d && (dstattr & 0xF) && (pixeladdr < BufferSize))
                 PlotTranslucentPixel(gpu.GPU3D, pixeladdr+BufferSize, color, z, polyattr, polygon->IsShadow);
         }
     }
@@ -1282,8 +1282,7 @@ void SoftRenderer::RenderPolygonScanline(const GPU& gpu, RendererPolygon* rp, s3
             if (!(polygon->Attr & (1<<11))) z = -1;
             PlotTranslucentPixel(gpu.GPU3D, pixeladdr, color, z, polyattr, polygon->IsShadow);
 
-            // blend with bottom pixel too, if needed
-            if ((dstattr & 0xF) && (pixeladdr < BufferSize))
+            if (!fast2d && (dstattr & 0xF) && (pixeladdr < BufferSize))
                 PlotTranslucentPixel(gpu.GPU3D, pixeladdr+BufferSize, color, z, polyattr, polygon->IsShadow);
         }
     }
@@ -1381,8 +1380,7 @@ void SoftRenderer::RenderPolygonScanline(const GPU& gpu, RendererPolygon* rp, s3
             if (!(polygon->Attr & (1<<11))) z = -1;
             PlotTranslucentPixel(gpu.GPU3D, pixeladdr, color, z, polyattr, polygon->IsShadow);
 
-            // blend with bottom pixel too, if needed
-            if ((dstattr & 0xF) && (pixeladdr < BufferSize))
+            if (!fast2d && (dstattr & 0xF) && (pixeladdr < BufferSize))
                 PlotTranslucentPixel(gpu.GPU3D, pixeladdr+BufferSize, color, z, polyattr, polygon->IsShadow);
         }
     }
@@ -1449,6 +1447,9 @@ u32 SoftRenderer::CalculateFogDensity(const GPU3D& gpu3d, u32 pixeladdr) const
 
 void SoftRenderer::ScanlineFinalPass(const GPU3D& gpu3d, s32 y)
 {
+    if (gpu3d.Fast2DApprox())
+        return;
+
     // to consider:
     // clearing all polygon fog flags if the master flag isn't set?
     // merging all final pass loops into one?
